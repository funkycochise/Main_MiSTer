Main mister fork, to disbale rom program bar loading.
it's disbled by default.
it equals to progress_info=0 in Mister.ini
if you want to enable it anyway, just add :
progress_info=1

by default, this fork also set 
disable_autofire=1;
so any wrong activation is avoided
